---
/**
 * Profile detail shell for D1-only slugs (no static path at build time).
 * Worker rewrites missing /directorio/{slug}/ → this page; client reads slug from the URL.
 */
import BaseLayout from "../../layouts/BaseLayout.astro";
import ProfileDetailClient from "../../components/ProfileDetailClient.astro";
---

<BaseLayout title="Perfil — DIMELA.mx" headerVariant="simple" homeHref="/">
  <ProfileDetailClient />
</BaseLayout>
